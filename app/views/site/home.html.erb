<%= render 'questions/form' %> 

<%- @questions.each do |question| %>
  <%= link_to question.title, question_answers_path(question) %>
  <%= question.description %>
  <%= question.user.email %>
  <%= question.answers.count %>
  <%= question.created_at %>
<% end -%>
